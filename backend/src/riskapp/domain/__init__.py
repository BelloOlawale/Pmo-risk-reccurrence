"""Pure domain logic: scoring, status lifecycle, and SLA rules.

Nothing in this package performs I/O (no database, HTTP, or filesystem access).
Every module here is deterministic and unit-tested.
"""
